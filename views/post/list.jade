extends ../layout

block content
	div
		a(href="/") Home
		br
		a(href="/create/post") Create Post
	each post in posts
		div(class="post")
			h4(class="title")
				a(href="/post/#{post._id}")= post.title
			p(class="author") 作者：#{post.title}
			p(class="body")= post.body
			a(href="/remove/post/#{post._id}") Delete
			&nbsp;|&nbsp;
			a(href="/update/post/#{post._id}") Save
